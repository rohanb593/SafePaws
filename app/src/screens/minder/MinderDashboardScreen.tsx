// Pet Minder home screen
//
// Uses: useBookings → fetchBookings(), useAuth → user
// Fetches bookings filtered to minder_id === uid
// Displays: pending requests count, upcoming confirmed bookings
//
// Elements:
//   BookingCard list (pending requests)
//   BookingCard list (confirmed upcoming)
//   Button ('View Job Requests' → JobRequestsScreen)
//   Button ('Manage Availability' → AvailabilityScreen)
//   Button ('Edit Profile' → MinderProfileEditScreen)
